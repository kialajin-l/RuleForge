{
    "contributes": {
        "configuration": {
            "properties": {
                "ruleforge.autoTrigger": {
                    "description": "Automatically extract rules when save threshold or idle timeout is reached",
                    "default": true,
                    "type": "boolean"
                },
                "ruleforge.defaultRepo": {
                    "description": "Default GitHub repo for rule contributions",
                    "default": "multiagent/rules",
                    "type": "string"
                }
            },
            "title": "RuleForge"
        },
        "commands": [
            {
                "command": "ruleforge.extract",
                "title": "RuleForge: Extract Rules from Session"
            },
            {
                "command": "ruleforge.viewRules",
                "title": "RuleForge: View Local Rules"
            },
            {
                "command": "ruleforge.syncRules",
                "title": "RuleForge: Sync Rules to AI Context"
            }
        ]
    },
    "scripts": {
        "package": "vsce package",
        "build": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node",
        "watch": "npm run build -- --watch"
    },
    "description": "Extract and share coding rules from your Trae sessions",
    "version": "1.0.0",
    "activationEvents": [
        "onLanguage",
        "onCommand:ruleforge.extract",
        "onCommand:ruleforge.viewRules",
        "onCommand:ruleforge.syncRules",
        "onStartupFinished"
    ],
    "engines": {
        "trae": "^1.0.0",
        "vscode": "^1.85.0"
    },
    "name": "ruleforge-adapter-trae",
    "main": "./dist/extension.js",
    "publisher": "ruleforge",
    "categories": [
        "Other"
    ],
    "devDependencies": {
        "@ruleforge/core": "file:../core",
        "@types/vscode": "^1.85.0",
        "@vscode/vsce": "^3.9.1",
        "esbuild": "^0.20.0",
        "js-yaml": "^4.1.0"
    },
    "displayName": "RuleForge for Trae"
}